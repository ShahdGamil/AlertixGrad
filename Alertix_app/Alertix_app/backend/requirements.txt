# ============================================================================
# Alertix YOLO Theft Detection API - Python Dependencies
# ============================================================================
# Install all dependencies with: pip install -r requirements.txt
# For Raspberry Pi, use: pip3 install -r requirements.txt
# ============================================================================

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6  # Required for file uploads

# YOLO and Computer Vision
ultralytics==8.1.0  # YOLOv8 and YOLO models
opencv-python-headless==4.9.0.80  # OpenCV without GUI (lighter for Raspberry Pi)
torch==2.1.2  # PyTorch (use CPU version for Raspberry Pi)
torchvision==0.16.2

# Image Processing
Pillow==10.2.0
numpy==1.24.3

# HTTP and CORS
httpx==0.26.0
starlette==0.35.1

# Logging and Utilities
python-dateutil==2.8.2

# Optional: For better performance monitoring
psutil==5.9.7

# ============================================================================
# INSTALLATION NOTES
# ============================================================================
#
# For Raspberry Pi (Recommended):
# --------------------------------
# 1. Install system dependencies first:
#    sudo apt-get update
#    sudo apt-get install python3-pip python3-dev
#    sudo apt-get install libopencv-dev python3-opencv
#    sudo apt-get install libatlas-base-dev gfortran
#
# 2. Install PyTorch CPU version:
#    pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# 3. Install other requirements:
#    pip3 install -r requirements.txt
#
# For Desktop/Server (with GPU):
# --------------------------------
# 1. Install CUDA-enabled PyTorch first:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# 2. Install other requirements:
#    pip install -r requirements.txt
#
# For Windows:
# ------------
# pip install -r requirements.txt
#
# ============================================================================
